import React from 'react';

//image
//hover feature? -> with just the time and nothing else

// square -> link with id -> specs in the inner recipe component

const Square = () => {
    return (
        <div class="nested">asd</div>
    )
}

export default Square